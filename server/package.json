{
  "name": "server",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/morgan": "^1.9.10",
    "@types/node": "^24.3.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clerk/express": "^1.7.30",
    "@prisma/client": "^6.16.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1",
    "prisma": "^6.16.1",
    "zod": "^4.1.8"
  },
  "scripts": {
    "start": "bun run prisma generate && bun run prisma db push && bun src/index.ts",
    "dev": "bun run prisma generate && bun run prisma db push && bun --watch src/index.ts",
    "migrate": "bun run prisma migrate dev",
    "studio": "bun run prisma studio"
  }
}
